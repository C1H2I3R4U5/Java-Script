Password Checker:
Build a program that checks whether a given password meets the following criteria:

At least 8 characters long
Contains at least one uppercase letter
Contains at least one lowercase letter
Contains at least one digit 


var str = "Horray"
if(str.length > 8)
   if(/[A-Z]/.test(str))
     if(/[a-z]/.test(str))
       console.log("password mets certia")
       else
         console.log("password does not met certia")
        else
        console.log("password does not met certia")
    else
    console.log("password does not met certia")
