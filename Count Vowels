function main() {
    var str = "hello"
    console.log(countVowels(str));
}
function countVowels(str) {
  // Your code goes here
  let count = 0
  for(let i=0;i<str.length;i++){
    
  if(str[i].match(/[aeiou]/)){
    count++
  }
}
return count
}
main()
