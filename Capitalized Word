//Capitalized Word

function main() {
    var str = readLine();
    console.log(capitalizeWords(str));
}
function capitalizeWords(str) {
  // Your code goes here
  var c = str.split(" ").map(word => {return word.charAt(0).toUpperCase()+word.slice(1)}).join(" ")
  return c
}
